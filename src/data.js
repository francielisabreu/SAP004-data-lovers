export const pokemon = (data) => {
  const pokemonImage = data.pokemon.map((imageTake) => {
    return imageTake;
  });
  return pokemonImage;
};

export const anotherExample = () => {
  return "OMG";
};
